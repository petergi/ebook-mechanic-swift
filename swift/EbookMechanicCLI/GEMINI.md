# Gemini's Role in EbookMechanicCLI

This file outlines the role of the Gemini AI assistant in the EbookMechanicCLI package.

## Project Understanding

I understand that this directory contains the `EbookMechanicCLI` package, which is the full-featured command-line interface for the Swift implementation of the EbookMechanic project. This CLI provides a terminal-based interface to all the functionality of the `EbookMechanicCore` library.

Key features of this CLI include:

*   A text-based interface with progress feedback.
*   Parsing of command-line flags and options.
*   Interactive prompts for user confirmation.
*   Support for shell completion.

This package depends on `EbookMechanicCore` for its underlying functionality.

## My Role

My purpose is to assist in the development and maintenance of the `EbookMechanicCLI` package. I can help with a variety of tasks, including:

*   **Adding New Commands and Options:** Extending the CLI with new commands and options to expose new features from the core library, such as `--max-concurrent`, `--no-cache`, and `--performance-stats`.
*   **Improving the User Interface:** Enhancing the usability and appearance of the command-line interface.
*   **Implementing New Features:** Adding new features to the CLI, such as new reporting formats or output options.
*   **Writing Tests:** Creating new tests to ensure the correctness of the CLI's behavior.
*   **Writing Documentation:** Documenting the CLI's commands, options, and usage.

I will adhere to the existing coding style and architectural patterns of the package, with a focus on creating a user-friendly and powerful command-line experience.
